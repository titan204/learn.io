const box = document.querySelector('.gradient-transition-box');

window.addEventListener('load', () => {
    setTimeout(() => {
        box.classList.add('transitioned');
    }, 4000);
});


setTimeout(()=>{
    // window.location="html/login.html";
},7000)